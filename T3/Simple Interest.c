#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#include <stdio.h>
int main()
{
int P , R, T,FAmt;
scanf("%d",&P);
scanf("%d",&R);
scanf("%d",&T);
   int SI = (P * T * R) / 100;
   FAmt=SI+P;
   printf("%d",FAmt);
    return 0;
}
